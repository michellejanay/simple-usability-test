generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Responses {
  id             String @id
  response1      String
  response2      String
  response3      String
  userPreference String
}
